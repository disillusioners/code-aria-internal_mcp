# MCP Guidelines Server Configuration
# Copy this file to .env and update with your actual values

# Database Configuration
# PostgreSQL connection string for the guidelines database
# Format: postgres://user:password@host:port/dbname?sslmode=disable
# This should point to the same database as the API/Worker services
GUIDELINES_DB_DSN=postgres://postgres:password@localhost:5432/codearia?sslmode=disable

# Optional: Logging Configuration
# Log level: debug, info, warn, error (default: info)
# GUIDELINES_LOG_LEVEL=info








