# PostgreSQL Master Database Configuration
# Connection string for the master PostgreSQL database
# This database is used to store connection configurations in the mcp_connections table
# The master connection is automatically created with the name "master" on startup
# Format: postgres://user:password@host:port/dbname?sslmode=disable
POSTGRES_DB_DSN=postgres://postgres:password@localhost:5432/postgres?sslmode=disable

# Note: Additional database connections can be managed via the MCP tools:
# - create_connection: Add new connections
# - list_connections: View all configured connections
# - get_connection: Get connection details
# - update_connection: Update connection settings
# - delete_connection: Remove connections (master cannot be deleted)




