{
  "mcpServers": {
    "mcp-postgres": {
      "command": "/home/nea/bin/mcp-postgres",
      "args": [],
      "env": {
        "POSTGRES_DB_DSN": "postgres://user:password@localhost:5432/dbname?sslmode=disable"
      }
    }
  }
}

